import React from "react";
import styled from "styled-components";

const HeaderStyles = styled.header``;

const Header = () => {
  return (
    <HeaderStyles>
      <a srcSet="/imges/logo.png 2x" />
    </HeaderStyles>
  );
};

export default Header;
